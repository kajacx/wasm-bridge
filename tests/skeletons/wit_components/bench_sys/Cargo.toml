[package]
name = "wit-components-bench-sys"
version = "0.1.0"
edition = "2021"

[dependencies]
wasm-bridge = { path = "../../../crates/wasm-bridge", default-features = false, features = ["component-model"] }
easybench = { version = "1.1" }
